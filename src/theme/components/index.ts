import { buttons } from "./button";
import { text } from "./text";
import { containers } from "./containers";
import { inputs, select } from "./input";
import { navigations } from "./navigation";

export const components = {
  ...buttons,
  ...text,
  ...containers,
  ...inputs,
  ...select,
  ...navigations,
};
