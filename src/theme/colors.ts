import { Colors } from "@/base/constants";
import { colorsTuple } from "@mantine/core";

export const colors = {
  main: colorsTuple(Colors.main),
  stroke: colorsTuple(Colors.stroke),
  peach: colorsTuple(Colors.peach),
  red: colorsTuple(Colors.red),
  grey: colorsTuple(Colors.grey),
  headBlue: colorsTuple(Colors.headBlue),
  reportInputActive: colorsTuple(Colors.reportInputActive),
  plat: colorsTuple(Colors.plat),
  black: colorsTuple(Colors.black),
  selectHover: colorsTuple(Colors.selectHover),
  pink: colorsTuple(Colors.pink),
  pinkWhite: colorsTuple(Colors.pinkWhite),
  darkBlue: colorsTuple(Colors.darkBlue),
  lightIvory: colorsTuple(Colors.lightIvory),
  deepMose20: colorsTuple(Colors.deepMose20),
  midnightBlue: colorsTuple(Colors.midnightBlue),
  mainGrey: colorsTuple(Colors.mainGrey),
};
